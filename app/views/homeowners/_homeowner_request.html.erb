<h2>Create a New Request</h2>

<%= form_with(model: [@homeowner, @homeowner_request], local: true) do |form| %>
  <% if @homeowner_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@homeowner_request.errors.count, "error") %> prohibited this request from being saved:</h2>
      <ul>
        <% @homeowner_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit "Create Request" %>
  </div>
<% end %>